1. Only one image library needs to be introduced.  
2. It is recommended to introduce a non-PDA image library first, and consider introducing a PDA image library if any issues occur during use.  
3. The PDA image library does not support functions such as setting text fonts, word wrapping, QR code error correction level, and LOGO.