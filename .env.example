# Ejemplo de variables de entorno
DATABASE_URL=postgresql+psycopg://inventory_user:inventory_pass@localhost:5432/inventory_db
JWT_SECRET=change_me_super_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# SMTP (Mailgun / Gmail / Sendgrid)
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=postmaster@sandbox.example.mailgun.org
SMTP_PASSWORD=CHANGE_ME
SMTP_FROM=postmaster@sandbox.example.mailgun.org
SMTP_TO=you@example.com
SMTP_USE_TLS=true
